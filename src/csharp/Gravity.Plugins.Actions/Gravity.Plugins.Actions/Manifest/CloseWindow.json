{
  "description": "Close the given window, quitting the browser if it is the last window currently open.",
  "examples": [
    {
      "actionExample": {
        "action": "CloseWindow",
        "argument": "2"
      },
      "description": "Close the given window by it\"s position index, quitting the browser if it is the last window currently open.",
      "literalExample": "close window {2}"
    }
  ],
  "name": "CloseWindow",
  "pluginType": "Action",
  "properties": {
    "argument": "The window (zero based) index while 0 is the first (main) window."
  },
  "protocol": {
    "endpoint": "/session/{session-id}/window",
    "w3c": "https://www.w3.org/TR/webdriver/#close-window"
  },
  "scope": [ "web", "mobile-web" ],
  "summary": "Close the given window/",
  "testOn": "https://gravitymvctestapplication.azurewebsites.net/instructor"
}