{
  "cliArguments": {
    "timeout": "Timeout to wait before throwing [TimeoutException], value can be TimeSpan [hh:mm:ss] or in millisecond [3000]. If not provided, default will be [PageLoadTimeout]."
  },
  "description": "Wait until the provided URL conditions are met.",
  "examples": [
    {
      "description": "Wait for url to match [.*] or until [PageLoadTimeout] reached.",
      "literalExample": "wait for url",
      "actionExample": {
        "action": "WaitForUrl"
      }
    },
    {
      "description": "Wait for url to match [google] or until [5000ms] timeout reached.",
      "literalExample": "wait for url {{$ --timeout:5000}} filter {google}",
      "actionExample": {
        "action": "WaitForUrl",
        "argument": "{{$ --timeout:5000}}",
        "regularExpression": "google"
      }
    },
    {
      "description": "Wait for url to match [google] or until [1min] timeout reached.",
      "literalExample": "wait for url {{$ --timeout:00:01:00}} filter {google}",
      "actionExample": {
        "action": "WaitForUrl",
        "argument": "{{$ --timeout:00:01:00}}",
        "regularExpression": "google"
      }
    }
  ],
  "name": "WaitForUrl",
  "pluginType": "Action",
  "properties": {
    "regularExpression": "A pattern by which the extracted information will be evaluated. Returns the first match.",
    "argument": "The page primary conditions cli to meet. If not specified, will wait for the url to match [.*] or until [PageLoadTimeout] reached."
  },
  "protocol": {
    "endpoint": "none",
    "w3c": "none"
  },
  "scope": [ "web", "mobile-web" ],
  "summary": "Wait until the provided URL conditions are met.",
  "testOn": "https://gravitymvctestapplication.azurewebsites.net/instructor"
}